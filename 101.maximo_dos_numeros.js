// 101. Máximo de Dos Números con operador ternario

let a = 15;
let b = 25;

// Usamos el operador ternario para determinar el mayor
let max = (a > b) ? a : b;

console.log(`El mayor entre ${a} y ${b} es: ${max}`);
