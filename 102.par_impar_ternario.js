// 102. Par o Impar con operador ternario

let numero = 7;

// Usamos ternario para determinar si es par o impar
let resultado = (numero % 2 === 0) ? "Par" : "Impar";

console.log(`${numero} es: ${resultado}`);
